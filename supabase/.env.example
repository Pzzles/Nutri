# Copy to `supabase/.env` for local Edge Functions. Never commit real values.

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Meal parsing provider. Leave GROQ_API_URL empty in production to use Groq's
# official OpenAI-compatible endpoint. GROQ_MODEL overrides the default model;
# check Groq's model list for a current supported ID if the default is deprecated.
GROQ_API_KEY=
GROQ_API_URL=
GROQ_MODEL=

FATSECRET_CONSUMER_KEY=
FATSECRET_CONSUMER_SECRET=

# Optional; the application falls back to USDA's rate-limited DEMO_KEY.
USDA_FDC_API_KEY=

CRON_SECRET=
